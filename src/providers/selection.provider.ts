import { createSignal } from 'solid-js';

export const [setSelectedHair, getSelectedHair] = createSignal();
export const [setSelectedFace, getSelectedFace] = createSignal();
export const [setSelectedBrows, getSelectedBrows] = createSignal();
export const [setSelectedEyes, getSelectedEyes] = createSignal();
export const [setSelectedGlasses, getSelectedGlasses] = createSignal();
export const [setSelectedNose, getSelectedNose] = createSignal();
export const [setSelectedMouth, getSelectedMouth] = createSignal();
export const [setSelectedFacialHair, getSelectedFacialHair] = createSignal();
